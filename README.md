# Andrew Robalino — Portfolio

Vite + React + Tailwind project with small inline demos for key projects.

## Run locally

```bash
npm install
npm run dev
```

Then open the printed URL (usually http://localhost:5173).

## Build for production

```bash
npm run build
npm run preview
```

On Netlify:

- **Build command:** `npm run build`
- **Publish directory:** `dist`
